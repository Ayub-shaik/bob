---
description: Activates the Bob forensic audit and correction protocol whenever user mentions bob or reports failed/shallow/regressed executions
alwaysApply: true
---

# Bob Audit & Correction Trigger

Whenever the user says **`bob`**, **`/bob`**, or indicates that a prior instruction was **not executed properly, half-done, or worsened other code**:

1. **Activate the `bob` skill immediately** (`~/.cursor/skills/bob/SKILL.md`).
2. **Halt normal code generation**. Do not jump into writing speculative code or producing polite excuses.
3. **Enforce Empirical Verification**: Apply the rules in `~/.cursor/rules/empirical-verification-invariants.mdc`.
4. **Execute the 4-step forensic procedure**:
   - **Step 1**: Reconstruct the physical and behavioral requirements from raw user speech.
   - **Step 1.5**: Follow `graft-code-map.mdc`, `mnemosyne-memory.mdc`, and `token-diet.mdc`. Bob extras: oxlint pre-flight, empirical UI drivers, degradation-tier audit.
   - **Step 2**: Perform Causal Feedback-Loop Tracing (Container conflicts, layout shifts feeding into scroll triggers, compositor overhead under high-frequency updates, touch physics under zero-velocity releases).
   - **Step 3**: Produce the Forensic Gap Analysis (Surface Mimicry vs. Real Behavior vs. Active Failure Modes).
   - **Step 4**: Emit and execute concrete, numbered surgical correction orders (`FIX-001`, `FIX-002`) that break the oscillation loops and fix the physical interaction. Record turn telemetry via `bob-telemetry log`.
